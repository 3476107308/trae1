# 事项清单小程序需求文档

## 1. 项目背景

我正在学习使用 AI 辅助开发小程序，希望通过 Trae 一步一步完成一个简单但完整的事项清单小程序。

这个项目主要用于练习小程序开发流程、页面搭建、数据绑定、事件处理、状态切换、日期筛选和基础页面跳转。

开发过程中请不要一次性完成所有功能，而是按照本文档中的阶段逐步开发。每完成一个阶段，都要保证项目可以正常运行，方便我及时检查和调整。

## 2. 项目目标

开发一个“事项清单小程序”，用于记录每天要完成的待办事项。

核心功能包括：

- 添加待办事项
- 添加事项时可以打开详情面板，填写事项说明、日期、子任务和工作量
- 点击事项切换完成状态
- 完成事项时有简单点击反馈或动画效果
- 按日期记录和查看事项
- 查看事项总览，快速了解一个月内每天的事项分布
- 查看完成事项曲线，了解最近一段时间的完成趋势
- 区分未完成事项和已完成事项
- 提供个人说明页面
- 页面简洁、美观，适合手机端使用

## 3. 开发原则

请 Trae 按照以下原则进行开发：

- 每次只实现一个阶段的功能
- 每个阶段完成后都要保证代码可以运行
- 不要一次性大改所有文件
- 不要过度封装，代码要适合新手理解
- 优先使用简单、稳定、容易调试的实现方式
- 不引入复杂第三方库
- 数据先使用小程序页面内的本地数组完成基础功能，等界面和交互稳定后，再接入 WampServer 本地服务器
- 本地服务器优先使用 WampServer 自带的 Apache、MySQL 或 MariaDB、phpMyAdmin 管理数据
- 后端接口先使用简单 PHP 文件实现，不要一开始引入复杂后端框架
- 每一步完成后说明修改了哪些文件
- 每一步完成后说明如何测试
- 如果遇到不确定的需求，请先询问我，不要自行扩展复杂功能

## 4. 目标用户

目标用户是正在学习小程序开发的新手。

所以界面和代码都应该尽量清晰直观，避免复杂架构和高级写法。

## 5. 页面规划

### 5.1 首页

首页是事项清单的主要操作页面。

需要包含：

- 页面标题
- 当前选中的日期
- 前一天按钮
- 后一天按钮
- 待办输入框
- 添加按钮
- 添加事项详情面板入口
- 未完成事项列表
- 已完成事项列表
- 进入个人说明页面的入口

### 5.2 个人说明页

个人说明页用于展示简单的个人介绍和小程序说明。

需要包含：

- 页面标题：“个人说明”
- 自我介绍内容
- 小程序用途说明
- 返回首页的入口

示例文案：

```text
你好，我正在学习使用 AI 辅助开发小程序。这个事项清单小程序用于记录每天要完成的任务，并通过点击事项来标记完成状态。
```

### 5.3 事项总览页

事项总览页用于按月份查看每天的事项情况，帮助用户快速了解哪些日期有任务、哪些日期已经完成。

需要包含：

- 页面标题：“事项总览”
- 当前查看的月份
- 上个月按钮
- 下个月按钮
- 月历网格
- 每天的事项数量提示
- 每天的完成数量提示
- 点击某一天后，可以回到首页并查看该日期的事项

### 5.4 完成事项曲线页

完成事项曲线页用于展示最近一段时间每天完成事项的数量趋势。

需要包含：

- 页面标题：“完成事项曲线”
- 最近 7 天或 8 天的日期标签
- 每天完成事项数量
- 简单折线图或柱状图
- 今日完成数量
- 最近 7 天平均完成数量
- 简单的文字总结

### 5.5 添加事项详情面板

添加事项详情面板用于在创建事项前补充更完整的信息，界面可以参考用户提供的底部弹出面板效果。

触发方式：

- 用户在首页点击“添加”按钮后，不直接创建事项，而是先打开添加事项详情面板
- 如果输入框中已经有内容，则自动带入详情面板的事项标题
- 如果输入框为空，也可以打开详情面板，但保存时必须填写事项标题

需要包含：

- 顶部拖拽提示条或关闭区域
- 分类选择，默认显示“未分类”
- 右上角“添加”确认按钮
- 事项标题输入框，例如“准备做什么？”
- 事项描述输入框，例如“添加描述”
- 日期快捷选择：今天、明天、选择日期、没有日期
- 子任务入口，例如“添加子任务”
- 工作量或难度选择，例如“一般”“中等难度”“较高难度”
- 关闭或取消操作

交互要求：

- 面板从底部弹出，背景页面保持可见但弱化
- 打开面板时，标题输入框自动聚焦并弹出键盘
- 点击“添加”后保存事项并关闭面板
- 点击背景或关闭按钮时，如果没有输入内容，直接关闭
- 如果已经输入内容，关闭前可以先提示是否放弃编辑
- 第一版可以先实现标题、描述、日期、工作量，子任务可以先做入口和占位，不必完整实现

## 6. 数据结构设计

本项目没有云数据库管理，数据管理方案分两步进行：

1. 前期开发阶段：先使用小程序页面内的数组保存事项数据，方便学习页面、事件和状态切换。
2. 本地持久化阶段：使用 WampServer 作为本地服务器，通过 PHP 接口连接本地 MySQL 或 MariaDB 数据库。

本地服务器建议：

- WampServer 负责启动本地 Apache 服务
- MySQL 或 MariaDB 用于保存事项数据
- phpMyAdmin 用于查看、添加、修改和删除数据库数据
- 小程序通过 `wx.request` 请求本地 PHP 接口
- 开发者工具中可以临时关闭合法域名校验，方便访问本地接口
- 真机调试时，需要保证手机和电脑在同一局域网，并使用电脑局域网 IP 访问接口，例如 `http://192.168.x.x/todo-api`

每一条事项建议使用如下数据结构：

```js
{
  id: 1,
  title: "学习小程序",
  description: "完成首页事项列表和添加逻辑",
  completed: false,
  date: "2026-05-16",
  category: "未分类",
  difficulty: "normal",
  subtasks: [],
  createdAt: "2026-05-16 12:00:00",
  updatedAt: "2026-05-16 12:00:00"
}
```

字段说明：

- `id`：事项唯一标识
- `title`：事项内容
- `description`：事项说明或备注，可为空
- `completed`：是否完成，`false` 表示未完成，`true` 表示已完成
- `date`：事项所属日期，格式为 `YYYY-MM-DD`
- `category`：事项分类，默认值为“未分类”
- `difficulty`：工作量或难度，建议值为 `normal`、`medium`、`hard`
- `subtasks`：子任务列表，第一版可以先保留为空数组，后续再扩展
- `createdAt`：创建时间，便于后续排查和排序
- `updatedAt`：更新时间，便于记录事项最后修改时间

本地数据库表建议：

```sql
CREATE DATABASE todo_app DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE todo_app;

CREATE TABLE todos (
  id INT AUTO_INCREMENT PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  description TEXT NULL,
  completed TINYINT(1) NOT NULL DEFAULT 0,
  date DATE NOT NULL,
  category VARCHAR(50) NOT NULL DEFAULT '未分类',
  difficulty VARCHAR(20) NOT NULL DEFAULT 'normal',
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

本地接口建议：

```text
GET    /todo-api/list.php?date=2026-05-16
POST   /todo-api/add.php
POST   /todo-api/toggle.php
POST   /todo-api/delete.php
GET    /todo-api/month-summary.php?month=2026-05
GET    /todo-api/completed-trend.php?days=7
```

`add.php` 新增事项时建议接收字段：

```json
{
  "title": "学习小程序",
  "description": "完成添加事项详情面板",
  "date": "2026-05-16",
  "category": "未分类",
  "difficulty": "normal"
}
```

接口返回格式建议统一：

```json
{
  "success": true,
  "data": [],
  "message": ""
}
```

注意：

- 本地接口只用于学习和本机调试，不作为正式线上服务
- PHP 接口中需要处理中文编码，统一使用 `utf8mb4`
- 接口返回 JSON 时需要设置 `Content-Type: application/json; charset=utf-8`
- SQL 查询中不要直接拼接用户输入，后续连接数据库时优先使用预处理语句

## 7. 功能需求

### 7.1 添加待办事项

用户可以在输入框中输入事项内容，然后点击“添加”按钮。

功能要求：

- 输入框为空时，不允许添加
- 输入为空时提示：“请输入事项内容”
- 添加成功后，事项显示在当前日期的未完成列表中
- 添加成功后，输入框自动清空
- 新增事项默认是未完成状态
- 新增事项属于当前选中的日期

### 7.1.1 添加事项详情面板（底部弹出）

用户点击首页”添加”按钮后，从底部弹出一个详情面板，可以对事项做更完整的说明。面板以全屏或接近全屏的方式滑入，覆盖在首页上方。

#### 面板布局（从上到下）

```
┌──────────────────────────────┐
│  ← 添加事项            [添加] │  ← 标题栏：左侧返回箭头 + 标题 + 右侧保存按钮
├──────────────────────────────┤
│                              │
│  [学习小程序            ]    │  ← 事项名称输入框（预填首页输入框内容）
│                              │
│  ┌──────────────────────┐   │
│  │ 添加描述              │   │  ← 多行文本域，浅灰背景
│  │                      │   │
│  └──────────────────────┘   │
│                              │
│  日期                       │  ← 分区标签
│  [今天] [明天] [选择日期] [没有日期] │  ← 4 个选项按钮，水平排列，选中高亮蓝色
│                              │
│  分类                       │  ← 分区标签
│  [未分类] [一般] [中等难度] [较高难度] │  ← 4 个选项按钮，水平排列，选中高亮蓝色
│                              │
│  [+ 添加子任务]              │  ← 占位按钮，第一版点击无实际功能
│                              │
└──────────────────────────────┘
```

#### 交互行为

1. **打开面板**：点击首页”添加”按钮 → 面板从底部滑入（使用 `wx.pageScrollTo` 或 CSS `transform` 动画），覆盖首页
2. **标题预填**：如果首页输入框有内容，自动带入面板的标题输入框；首页输入框为空时，面板标题输入框也为空
3. **日期选择**：默认选中”今天”，用户可切换为”明天”、”选择日期”（调起系统日期选择器）、”没有日期”（事项不绑定日期）
   - 第一版可隐藏”没有日期”选项，始终绑定一个日期
4. **难度/分类选择**：默认选中”未分类”，单选，四选一
5. **保存**：点击右上角”添加”按钮 → 校验标题非空 → 调用 `POST /todo-api/add.php` 传入 `{title, description, date, category, difficulty}` → 关闭面板 → 首页刷新列表 → 输入框清空
6. **取消**：点击左上角返回箭头 或 点击面板外部遮罩 → 关闭面板，不保存，不清空首页输入框
7. **关闭动画**：面板向下滑出

#### 数据结构扩展

事项原有字段不变，通过此面板新增的字段：

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `description` | string | `””` | 事项描述文本 |
| `category` | string | `”未分类”` | 分类：未分类 / 一般 / 中等难度 / 较高难度 |
| `difficulty` | string | `”normal”` | 难度等级：normal / medium / hard |

对应的 PHP `add.php` 需要扩展 INSERT 语句，将 `description`、`category`、`difficulty` 一并写入数据库。

#### UI 样式要求

- 面板背景白色，顶部圆角 20rpx
- 标题栏：高度 100rpx，左侧箭头 48rpx 深灰色，标题”添加事项”居中 36rpx 加粗，右侧”添加”按钮蓝色文字 30rpx
- 输入框：边框 1px solid #e5e5e5，圆角 12rpx，内边距 24rpx，字号 32rpx
- 文本域：最小高度 160rpx，背景 #f5f5f5，圆角 12rpx，内边距 24rpx，字号 30rpx，placeholder “添加描述”
- 选项按钮：灰色背景 #f5f5f5，圆角 8rpx，内边距 16rpx 24rpx，字号 28rpx；选中态蓝色背景 #4a90e2 白色文字
- 分区标签：字号 28rpx，颜色 #999，上下间距 24rpx
- “添加子任务”按钮：文字按钮，蓝色文字 #4a90e2，字号 28rpx
- 面板内容区域可滚动（`scroll-view`），防止键盘弹出时内容被遮挡

#### 验收标准

- [ ] 点击首页”添加”后，面板从底部滑入
- [ ] 首页输入框有内容时，面板标题输入框自动填入该内容
- [ ] 标题为空时点击”添加”，toast 提示”请输入事项内容”
- [ ] 填写标题、描述、选择日期和难度后点击”添加”，事项出现在当前日期未完成列表中
- [ ] 事项数据中包含 `title`、`description`、`date`、`category`、`difficulty` 完整字段
- [ ] 点击返回箭头或面板外遮罩，面板关闭，不保存
- [ ] 面板关闭后首页输入框保持原内容不清空；但成功添加后首页输入框清空
- [ ] 日期选择器默认”今天”，切换”明天”后日期正确变更
- [ ] 难度选择默认”未分类”，点击其他选项后切换选中态样式
- [ ] 键盘弹出时面板内容可滚动，不被遮挡

### 7.2 点击切换完成状态

用户点击某一条事项，可以切换该事项的完成状态。

功能要求：

- 未完成事项点击后变为已完成
- 已完成事项点击后可以恢复为未完成
- 已完成事项文字显示删除线
- 已完成事项颜色变浅
- 不要因为点击完成而删除事项

### 7.3 点击特效

事项被点击时，需要有简单的交互反馈。

功能要求：

- 点击事项时有明显反馈
- 可以使用缩放、颜色变化、勾选图标变化等简单效果
- 未完成事项前面显示空圆圈
- 已完成事项前面显示对勾
- 动画不要太复杂，保证运行稳定

### 7.4 按日期记录事项

每条事项都要记录所属日期。

功能要求：

- 页面默认显示今天的日期
- 默认显示今天的事项
- 每个事项添加时记录当前选中的日期
- 事项列表只显示当前选中日期下的事项
- 日期格式使用 `YYYY-MM-DD`

### 7.5 切换日期

首页需要支持查看不同日期的事项。

功能要求：

- 页面顶部显示当前选中的日期
- 有“前一天”按钮
- 有“后一天”按钮
- 点击“前一天”后，显示前一天的事项
- 点击“后一天”后，显示后一天的事项
- 不同日期的事项互不影响
- 添加事项时，事项应该属于当前选中的日期

### 7.6 区分未完成和已完成

事项列表需要按照完成状态分区展示。

功能要求：

- 先显示“未完成”
- 再显示“已完成”
- 未完成事项显示在上方
- 已完成事项显示在下方
- 如果某一类没有事项，显示简单空状态提示

示例：

```text
未完成
- 买菜
- 学习

已完成
- 整理房间
```

### 7.7 个人说明页面

小程序需要有一个个人说明页面。

功能要求：

- 首页提供进入个人说明页的入口
- 个人说明页展示自我介绍
- 个人说明页展示小程序用途
- 个人说明页提供返回首页的入口
- 页面风格和首页保持一致

### 7.8 删除功能，可选

删除功能不是第一阶段必须功能，可以等主要功能稳定后再添加。

如果添加删除功能，要求如下：

- 每条事项右侧显示删除按钮
- 点击删除按钮后弹出确认提示
- 确认后删除该事项
- 删除操作不能影响其他日期的事项
- 注意避免点击删除按钮时同时触发完成状态切换

### 7.9 总览事项

总览事项功能用于按月份展示事项分布，类似一个简单日历总览。

功能要求：

- 默认显示当前月份
- 支持切换上个月和下个月
- 每一天显示日期数字
- 如果当天有事项，显示事项数量
- 如果当天有已完成事项，显示完成数量或完成标记
- 点击某一天后，跳转或返回首页，并把首页日期切换到对应日期
- 没有事项的日期保持简洁，不显示多余内容
- 总览页只做轻量展示，不在该页面直接编辑事项

示例：

```text
2026 年 5 月

16
事项 2
完成 1
```

### 7.10 完成事项曲线

完成事项曲线功能用于展示最近一段时间每天完成事项的数量趋势。

功能要求：

- 默认统计最近 7 天
- 只统计 `completed: true` 的事项
- 按事项所属日期 `date` 进行统计
- 每个日期显示完成事项数量
- 如果没有完成事项，曲线仍然正常显示，数值为 0
- 显示今日完成数量
- 显示最近 7 天平均完成数量
- 显示一段简短总结，例如”今天完成 3 项，高于最近 7 天平均水平”
- 图表实现要简单稳定，不引入复杂第三方库

统计示例：

```js
[
  { date: “2026-05-10”, completedCount: 2 },
  { date: “2026-05-11”, completedCount: 0 },
  { date: “2026-05-12”, completedCount: 3 }
]
```

#### 7.10.1 图表展示要求

图表区域需要同时包含**柱状图**和**折线图**两个图层，叠放在一起展示：

**柱状图：**
- 每个日期对应一根柱子
- 柱子高度与完成数量成正比
- 柱子颜色使用蓝色渐变（`#4a90e2` → `#3a7ac7`）
- 柱子顶部显示完成数量数字

**折线图（Canvas 2D 绘制）：**
- 使用微信小程序原生 `<canvas type=”2d”>` 绘制
- 在柱状图上方叠加折线，连接 7 个数据点
- 折线颜色 `#4a90e2`，线宽 3px，线段连接处使用圆角（`lineJoin: 'round'`）
- 每个数据点画实心圆标记：半径 6px，填充 `#4a90e2`，白色描边 3px
- 折线下方区域使用半透明蓝色渐变填充（从 `rgba(74,144,226,0.2)` 渐变到 `rgba(74,144,226,0)`），增强视觉效果
- Canvas 尺寸需要乘以设备像素比（`devicePixelRatio`）避免模糊

**坐标计算：**
- X 轴：7 个数据点均匀分布在 canvas 宽度上，左右留 padding
- Y 轴：根据 `item.completedCount / maxCount` 计算高度比例，顶部留白给数值标签，底部留白给日期标签
- 如果 `maxCount === 0`（7 天均为 0），在 canvas 中央绘制”暂无数据”文案
- 如果只有 1 个数据点，只画圆点、不画折线

**响应式重绘：**
- 每次进入页面时重新绘制折线
- 切换事项完成状态后回到趋势页，数据刷新并重新绘制

**交互增强（可选）：**
- 折线上的数据点可点击，用 tooltip 显示当日具体完成数
- 数据点触摸时放大（1.5 倍），离开时恢复

#### 7.10.2 兼容方案

如果 Canvas 2D 绘制遇到兼容性问题，可以采用以下降级方案：

- 方案 A：使用多个 `<view>` 配合 CSS `transform: rotate()` 画线段（每两个相邻数据点之间一条线段）
- 方案 B：仅保留柱状图 + 数据点圆点（无连线），即当前已有的实现

优先使用 Canvas 2D，如果实现困难再降级。

### 7.11 本地数据管理

本地数据管理功能用于把事项从页面临时数组保存到 WampServer 本地数据库中。

功能要求：

- 使用 WampServer 启动本地 Apache 和 MySQL 或 MariaDB
- 使用 phpMyAdmin 创建和管理 `todo_app` 数据库
- 使用 `todos` 表保存事项数据
- 小程序通过 `wx.request` 调用 PHP 接口读取和保存数据
- 首页加载时，从接口读取当前日期的事项
- 添加事项时，调用接口保存到数据库
- 切换完成状态时，调用接口更新数据库
- 删除事项如果后续实现，也通过接口删除数据库记录
- 事项总览和完成事项曲线的数据，优先从接口统计结果获取
- 如果接口请求失败，需要显示简单提示，例如“本地服务器连接失败”

开发注意：

- 开发者工具调试时，可以在详情中勾选“不校验合法域名、web-view、TLS 版本以及 HTTPS 证书”
- 接口地址先统一写在一个配置位置，方便以后从 `localhost` 改成局域网 IP
- 电脑本机调试可以使用 `http://localhost/todo-api`
- 真机调试通常不能直接使用 `localhost`，需要改为电脑的局域网 IP
- 本地服务器阶段只做单人本地使用，不做登录和权限管理

### 7.12 编辑事项

用户可以修改已有事项的标题、描述、日期和难度分类。

功能要求：

- 在事项列表每一项右侧显示编辑入口（可使用 `⋯` 图标或"编辑"文字链接）
- 点击编辑入口后，底部弹出编辑面板（复用 7.1.1 的详情面板，所有字段回填当前值）
- 编辑面板可以修改：标题、描述、日期、难度分类
- 点击面板"保存"后，调用 `POST /todo-api/update.php` 更新数据库
- 保存成功后关闭面板，列表自动刷新
- 点击取消或遮罩关闭面板时，不保存修改
- 编辑面板标题栏显示"编辑事项"（而非"添加事项"）
- 保存按钮文案改为"保存"（而非"添加"）

验收标准：

- [ ] 事项旁能看到编辑入口
- [ ] 点击编辑后弹出面板，原有标题、描述、日期、难度已回填
- [ ] 修改内容后点击"保存"，列表中事项内容更新
- [ ] 取消编辑不改变原事项内容
- [ ] 编辑面板打开时标题栏显示"编辑事项"

### 7.13 按周查看任务

在首页增加一个周视图模式，用户可以切换"按天查看"和"按周查看"。

功能要求：

- 在日期切换区域增加"按天"/"按周"切换按钮（或 Tab）
- 默认"按天"：当前行为不变，只显示选中日期的事项
- 切换到"按周"：
  - 日期切换按钮变为"上一周"/"下一周"，以周为单位切换
  - 显示当前周范围文字（如"2026-05-11 ~ 2026-05-17"）
  - 事项列表按日期分组显示：每天一个分组，分组标题为日期 + 星期
  - 每个分组内仍然区分未完成和已完成
  - 没有事项的日期折叠或显示"该日无事事项"
- 统计区域（如今日完成/7日平均）在按周模式下仍然保留
- 点击某一天的分组标题，可以切换回"按天"模式并定位到该日期

周视图布局示例：

```
┌──────────────────────────────┐
│  按天 | 按周                   │  ← 切换 Tab
│  ‹  5/11 ~ 5/17  ›           │  ← 周范围 + 切换按钮
├──────────────────────────────┤
│  ▼ 5月16日 周六   共 2 项      │  ← 日期分组标题（可折叠）
│  ○ 学习小程序                  │
│  ○ 整理笔记                    │
│                              │
│  ▼ 5月15日 周五   共 1 项      │
│  ✓ 购买日用品                  │
│                              │
│  ▸ 5月14日 周四   无事项       │  ← 折叠的日期
└──────────────────────────────┘
```

验收标准：

- [ ] "按天"/"按周"切换按钮正常切换
- [ ] 按周模式下，上一周/下一周按钮正确切换周范围
- [ ] 事项按日期正确分组显示
- [ ] 从按周模式点某一天后，能切回按天模式并定位到该日期
- [ ] 两种模式下添加事项都绑定到正确的日期
- [ ] 无事项的日期显示占位文字

### 7.14 子任务完整管理

每个事项可以有若干子任务，用于拆解复杂任务。子任务有独立的完成状态，父任务的完成进度由子任务决定。

#### 数据结构

每条子任务的数据结构：

```js
{
  id: 1,                  // 子任务唯一标识
  todoId: 5,              // 所属父事项 ID
  title: "搭建页面结构",    // 子任务内容
  completed: false,       // 是否完成
  order: 0,               // 排序序号
  createdAt: "2026-05-16 10:00:00"
}
```

数据库新增 `subtasks` 表：

```sql
CREATE TABLE subtasks (
  id INT AUTO_INCREMENT PRIMARY KEY,
  todo_id INT NOT NULL,
  title VARCHAR(255) NOT NULL,
  completed TINYINT(1) NOT NULL DEFAULT 0,
  sort_order INT NOT NULL DEFAULT 0,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (todo_id) REFERENCES todos(id) ON DELETE CASCADE
);
```

新增接口：

```text
GET    /todo-api/subtasks.php?todo_id=5         → 获取某事项的所有子任务
POST   /todo-api/subtask-add.php                → 添加子任务 {todo_id, title}
POST   /todo-api/subtask-toggle.php             → 切换子任务完成状态 {id}
POST   /todo-api/subtask-delete.php             → 删除子任务 {id}
POST   /todo-api/subtask-reorder.php            → 调整排序 {id, new_order}
```

#### 交互行为

1. **添加入口**：在 7.1.1 详情面板中，点击"[+ 添加子任务]" → 下方出现子任务输入行
2. **添加子任务**：输入子任务标题 → 回车或点击确认 → 子任务出现在列表中
3. **切换完成**：点击子任务左侧圆圈 → 切换完成状态（复用主事项的勾选样式）
4. **删除子任务**：左滑子任务 → 出现红色删除按钮
5. **进度展示**：父事项右侧显示完成进度（如"2/5"表示 5 个子任务中完成了 2 个）
6. **自动完成**：所有子任务完成后，父事项自动标记为完成（可选，需要在设置中开启）
7. **列表内联展示**：首页事项列表中，有子任务的事项下方缩进显示子任务列表（最多显示 3 个 + "更多..."）

验收标准：

- [ ] 详情面板点击"[+ 添加子任务]"后，能输入子任务内容
- [ ] 添加的子任务显示在面板列表中
- [ ] 点击子任务切换完成状态
- [ ] 左滑删除子任务，有确认提示
- [ ] 首页事项有子任务时，下方显示进度（如 2/5）
- [ ] 删除父事项时，子任务一并删除（数据库级联删除）
- [ ] 子任务保存到数据库，重新打开小程序后不丢失

### 7.15 任务优先级

为每条事项设置优先级，帮助用户聚焦重要任务。优先级通过颜色和排序在列表中直观展示。

#### 优先级等级

| 等级 | 标识 | 颜色 | 说明 |
|------|------|------|------|
| P1 高 | `!!!` | 红色 `#e74c3c` | 今天必须完成 |
| P2 中 | `!!` | 橙色 `#f39c12` | 今天应该完成 |
| P3 低 | `!` | 灰色 `#999` | 有时间再做 |

默认优先级的数据库字段：

```sql
ALTER TABLE todos ADD COLUMN priority VARCHAR(4) NOT NULL DEFAULT 'P2';
```

#### 交互行为

1. **设置优先级**：在 7.1.1 详情面板中，增加"优先级"选择行，三个选项（高/中/低）水平排列
2. **列表内展示**：事项标题左侧显示优先级色条（4rpx 宽的竖条，颜色对应该优先级的颜色）
3. **排序规则**：事项列表中，未完成事项按优先级排序（P1 → P2 → P3），同优先级按创建时间排序；已完成事项不做优先级排序
4. **修改优先级**：通过编辑面板（7.12）修改优先级
5. **默认值**：新增事项默认优先级为 P2（中）

#### 列表中的视觉效果

```
┌──────────────────────────────┐
│  未完成              3       │
│                              │
│  ▌ 学习小程序                  │  ← P1 红色色条
│  ▌ 整理笔记                   │  ← P1 红色色条
│  ▌ 购买日用品                  │  ← P2 橙色色条
│  ▌ 散步                      │  ← P3 灰色色条
├──────────────────────────────┤
│  已完成              1       │
│  ✓ 复习昨天的代码              │
└──────────────────────────────┘
```

验收标准：

- [ ] 详情面板中能选择优先级（高/中/低）
- [ ] 事项列表按 P1 → P2 → P3 排序（未完成区域）
- [ ] 高优先级事项左侧有红色色条
- [ ] 中优先级事项左侧有橙色色条
- [ ] 低优先级事项左侧有灰色色条
- [ ] 编辑面板中能修改优先级
- [ ] 新增事项默认优先级为 P2

## 8. 分阶段开发计划

请严格按照以下阶段逐步开发。

### 阶段 1：搭建基础页面

目标：

- 创建首页
- 显示标题“我的事项清单”
- 显示输入框
- 显示“添加”按钮
- 显示空的事项列表区域

本阶段不要实现添加功能，只搭建页面结构和基础样式。

验收标准：

- 小程序可以正常运行
- 首页能正常显示
- 输入框和按钮布局正常
- 页面适合手机端显示

### 阶段 2：实现添加事项

目标：

- 用户输入内容后点击“添加”
- 新事项加入列表
- 输入框自动清空
- 空内容不能添加

验收标准：

- 输入普通文字后可以添加到列表
- 输入框为空时点击添加会提示
- 添加完成后输入框清空

### 阶段 2.1：添加事项详情面板（底部弹出）

目标：

- 点击首页”添加”按钮后，从底部弹出详情面板（覆盖在首页上方）
- 面板包含：标题输入框、描述文本域、日期选择（今天/明天/选择日期/没有日期）、难度选择（未分类/一般/中等难度/较高难度）、添加子任务入口
- 如果首页输入框有内容，自动预填到面板标题输入框
- 点击面板右上角”添加”后，校验标题非空，保存事项，关闭面板，首页输入框清空，列表刷新
- 点击左上角返回箭头或面板外遮罩，关闭面板，不保存，首页输入框内容保留
- 第一版”没有日期”和”添加子任务”可作为占位，不实现实际功能

本阶段先使用小程序本地数组（`data.todoList`）保存完整数据（含 `description`、`category`、`difficulty`），不需要立即接入 WampServer。等接入 WampServer 后（阶段 11），再将新增字段写入数据库。

修改文件：

- `pages/index/index.wxml`：在页面底部添加面板 WXML（使用 `wx:if` 控制显隐 + CSS transition 做滑入动画），外层遮罩 + 白色面板容器
- `pages/index/index.js`：
  - 新增 data 字段：`showDetailPanel: false`、`detailTitle: ''`、`detailDescription: ''`、`detailDate: 'today'`、`detailCategory: '未分类'`
  - 修改 `addTodo`：不再直接添加，改为 `this.setData({ showDetailPanel: true })` 并将 `inputValue` 带入 `detailTitle`
  - 新增 `closeDetailPanel()`：关闭面板
  - 新增 `saveDetailTodo()`：校验标题非空 → 构造完整事项对象 → 加入 `todoList` → 调用 `filterTodos()` → 关闭面板 → 清空 `inputValue`
  - 新增 `selectDetailDate(e)`：切换日期选项
  - 新增 `selectDetailCategory(e)`：切换难度选项
- `pages/index/index.wxss`：面板、遮罩、滑入动画、选项按钮等样式

面板样式要点（参照 7.1.1 节 UI 布局图）：

- 遮罩：`position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: rgba(0,0,0,0.5); z-index: 1000`
- 面板：`position: fixed; bottom: 0; left: 0; width: 100vw; max-height: 85vh; background: #fff; border-radius: 20rpx 20rpx 0 0; z-index: 1001`
- 标题栏：高度 100rpx，flex 布局，左箭头 + 标题”添加事项” + 右”添加”按钮
- 标题输入框：border 1px solid #e5e5e5，圆角 12rpx，字号 32rpx
- 描述文本域：最小高度 160rpx，背景 #f5f5f5，圆角 12rpx，placeholder “添加描述”
- 日期/难度选项按钮：灰色背景 #f5f5f5，圆角 8rpx，选中态蓝色背景 #4a90e2 白色文字
- 滑入动画：`transition: transform 0.3s ease`，从 `translateY(100%)` 到 `translateY(0)`

验收标准：

- [ ] 点击首页”添加”后，面板从底部滑入
- [ ] 首页输入框有内容时，面板标题输入框自动填入该内容
- [ ] 标题为空时点击面板”添加”，toast 提示”请输入事项内容”
- [ ] 填写标题和描述后点击”添加”，事项出现在当前日期未完成列表，数据包含 `description` 和 `category`
- [ ] 点击”明天”后，事项日期变为明天
- [ ] 点击返回箭头或遮罩，面板关闭，不保存
- [ ] 面板关闭后首页输入框保留原内容
- [ ] 成功添加后首页输入框清空
- [ ] 日期选项默认”今天”，切换后选中态样式正确
- [ ] 难度选项默认”未分类”，切换后选中态样式正确
- [ ] 键盘弹出时面板内容可滚动，不被遮挡

### 阶段 3：实现完成状态切换

目标：

- 点击事项切换完成状态
- 已完成事项显示删除线
- 已完成事项颜色变浅
- 再次点击可恢复未完成

验收标准：

- 点击未完成事项后样式变化
- 点击已完成事项后恢复原样
- 事项不会被误删

### 阶段 4：添加点击特效

目标：

- 为事项点击增加简单反馈
- 已完成事项前显示对勾
- 未完成事项前显示空圆圈

验收标准：

- 点击事项时有视觉反馈
- 完成和未完成状态区分明显
- 动画不影响正常点击

### 阶段 5：加入日期字段

目标：

- 每条事项记录日期
- 页面显示当前日期
- 默认显示今天的事项
- 添加事项时绑定当前日期

验收标准：

- 新增事项数据中包含日期
- 日期格式为 `YYYY-MM-DD`
- 当前页面只显示当前日期的事项

### 阶段 6：实现日期切换

目标：

- 添加“前一天”和“后一天”按钮
- 支持切换不同日期
- 不同日期的事项互不影响

验收标准：

- 点击前一天后日期正确减少一天
- 点击后一天后日期正确增加一天
- 在不同日期添加的事项只出现在对应日期下

### 阶段 7：分区显示事项

目标：

- 未完成事项显示在上方
- 已完成事项显示在下方
- 空列表显示提示

验收标准：

- 未完成和已完成分区清楚
- 切换完成状态后，事项能移动到对应区域
- 没有事项时能看到空状态提示

### 阶段 8：添加个人说明页

目标：

- 创建个人说明页面
- 首页添加入口
- 支持从个人说明页返回首页

验收标准：

- 首页可以进入个人说明页
- 个人说明页内容显示正常
- 可以返回首页
- 不影响首页事项功能

### 阶段 9：整体样式优化

目标：

- 优化页面视觉效果
- 保持简洁清爽
- 统一输入框、按钮、事项列表样式
- 保证手机端体验良好

验收标准：

- 页面层级清晰
- 按钮和列表样式统一
- 已完成和未完成状态明显
- 手机端不会出现文字重叠或布局错乱

### 阶段 10：问题检查和修复

目标：

- 检查所有核心功能
- 修复明显问题
- 不新增复杂功能

检查清单：

- 添加空事项是否会被阻止
- 添加事项后输入框是否清空
- 点击事项是否能切换完成状态
- 日期切换是否正确
- 不同日期的数据是否互不影响
- 未完成和已完成分区是否正确
- 个人说明页面是否能正常进入和返回
- 手机端样式是否正常

### 阶段 11：接入 WampServer 本地数据

目标：

- 在 WampServer 中创建本地数据库和 `todos` 表
- 创建简单 PHP 接口
- 小程序通过接口读取、添加和更新事项
- 保留原有页面交互体验

验收标准：

- WampServer 启动后，小程序可以读取当前日期事项
- 添加事项后，phpMyAdmin 中能看到新数据
- 点击事项切换完成状态后，数据库中的 `completed` 字段同步变化
- 日期切换后，接口只返回对应日期的事项
- 关闭并重新打开小程序后，已添加的事项仍然存在
- 如果 WampServer 未启动，小程序能给出简单错误提示

### 阶段 12：添加事项总览页

目标：

- 创建事项总览页面
- 按月份展示日期网格
- 在有事项的日期上显示事项数量和完成数量
- 点击日期后可以查看对应日期的事项
- 总览数据从本地接口或本地统计方法获取

验收标准：

- 默认显示当前月份
- 上个月和下个月切换正确
- 有事项的日期能显示数量
- 点击日期后首页能切换到对应日期
- 不影响首页原有添加、完成和日期切换功能

### 阶段 13：添加完成事项曲线（含折线图）

目标：

- 创建完成事项曲线页面（`pages/trend/trend`）
- 从 `GET /todo-api/completed-trend.php?days=7` 获取最近 7 天每天完成数量
- 柱状图展示每天完成数量
- 使用 Canvas 2D 在柱状图上方叠加折线，连接 7 个数据点
- 每个数据点绘制实心圆标记（半径 6px，蓝底白边）
- 折线下方区域用半透明渐变填充
- 显示今日完成数量和最近 7 天平均完成数量
- 显示简短文字总结（高于/低于/持平平均水平）

修改文件：

- `pages/trend/trend.wxml`：在图表区域添加 `<canvas type="2d">` 元素
- `pages/trend/trend.js`：新增 `drawLineChart()` 方法，在数据加载完成后调用 Canvas API 绘制折线；使用 `wx.createSelectorQuery()` 获取 canvas 尺寸，乘以设备像素比避免模糊
- `pages/trend/trend.wxss`：调整 canvas 容器样式定位

验收标准：

- 7 个数据点之间有蓝色折线连接
- 每个数据点上有圆点标记（蓝底白边）
- 折线下方有半透明渐变填充
- 柱状图和折线图数据一致
- 多次进出趋势页时折线正常重绘
- 7 天数据全为 0 时 canvas 显示"暂无数据"，不报错
- 只有 1 个数据点时只画圆点、不画折线
- 图表在手机端显示正常，不出现文字重叠或模糊

### 阶段 14：实现编辑事项

目标：

- 在首页事项列表每一项右侧添加编辑入口（`⋯` 图标按钮）
- 点击编辑入口后，底部弹出编辑面板（复用阶段 2.1 的详情面板组件，切换为编辑模式）
- 编辑面板自动回填事项当前的标题、描述、日期、难度
- 点击"保存"后，更新本地数组中的事项数据（接入 API 后调用 `POST /todo-api/update.php`）
- 编辑面板标题栏显示"编辑事项"，按钮文案为"保存"

修改文件：

- `pages/index/index.wxml`：每项事项右侧添加 `⋯` 编辑按钮（`bindtap="editTodo" data-id="{{item.id}}"`），注意阻止事件冒泡以免触发 toggleTodo
- `pages/index/index.js`：
  - 新增 `editTodo(e)`：根据 id 找到事项，回填字段到 `detailTitle/detailDescription/detailDate/detailCategory`，设置 `editingId`，打开面板
  - 修改 `saveDetailTodo()`：若 `editingId` 非空则更新已有事项而非新增
  - 修改 `closeDetailPanel()`：同时清空 `editingId`
  - 面板标题和按钮文案根据 `editingId` 动态变化
- `pages/index/index.wxss`：编辑按钮样式（灰色小字或图标，30rpx）

验收标准：

- [ ] 事项右侧能看到 `⋯` 编辑按钮
- [ ] 点击 `⋯` 后弹出面板，内容已回填
- [ ] 修改后点击"保存"，列表中事项更新
- [ ] 点击 `⋯` 不会同时触发事项完成状态切换
- [ ] 取消编辑不改变原事项

### 阶段 15：实现按周查看任务

目标：

- 在首页日期切换区域增加"按天"/"按周" Tab 切换
- 按周模式下，日期导航切换为"上一周"/"下一周"，显示周范围文字
- 事项列表按日期分组渲染，每天一个分组区块
- 点击分组标题可切回按天模式并定位到该日期
- 添加事项时绑定当前选中的日期（在按周模式下默认为今天）

修改文件：

- `pages/index/index.wxml`：新增 Tab 切换按钮、周范围显示、按天/按周两套列表渲染
- `pages/index/index.js`：
  - data 新增 `viewMode: 'day'`（'day' | 'week'）、`weekStart: ''`、`weekEnd: ''`、`weekTodos`（按日期分组的数据）
  - 新增 `switchToDayMode()` / `switchToWeekMode()`：切换模式并重新加载数据
  - 新增 `prevWeek()` / `nextWeek()`：周切换
  - 新增 `formatWeekRange()`：生成周范围文字
  - 新增 `groupTodosByDate()`：将事项按日期分组
  - 新增 `selectWeekDay(e)`：点分组标题切回按天模式
  - 修改 `loadTodos`：按周模式下加载本周 7 天数据
- `pages/index/index.wxss`：Tab 样式、分组标题样式、折叠样式

验收标准：

- [ ] "按天"/"按周" Tab 切换正常
- [ ] 按周模式下，上一周/下一周切换正确
- [ ] 事项按日期正确分组
- [ ] 有事项的日期展开显示，无事项的日期折叠
- [ ] 点击分组标题可切回按天模式并定位到对应日期
- [ ] 按周模式下添加事项绑定到正确的日期

### 阶段 16：实现子任务完整管理

目标：

- 数据库新建 `subtasks` 表（按 7.14 节 SQL 定义）
- 新建 `subtasks.php`、`subtask-add.php`、`subtask-toggle.php`、`subtask-delete.php` 四个 PHP 接口
- 详情面板中"[+ 添加子任务]"实现实际功能：输入子任务标题、回车添加、点击切换完成、左滑删除
- 首页事项列表中，有子任务的事项下方缩进显示子任务进度

修改文件：

- `todo-api/subtasks.php`（新增）：GET 接口，根据 `?todo_id=` 返回子任务列表
- `todo-api/subtask-add.php`（新增）：POST 接口，接收 `{todo_id, title}`，INSERT 后返回新子任务
- `todo-api/subtask-toggle.php`（新增）：POST 接口，接收 `{id}`，切换 `completed` 字段
- `todo-api/subtask-delete.php`（新增）：POST 接口，接收 `{id}`，DELETE 子任务
- `pages/index/index.wxml`：详情面板中子任务列表区域；首页列表中有子任务的事项下方缩进显示子任务
- `pages/index/index.js`：
  - 新增 `addSubtask()`、`toggleSubtask()`、`deleteSubtask()`
  - 修改 `saveDetailTodo()`：保存后也保存子任务列表
  - 修改 `loadTodos()`：同时加载每个事项的子任务
  - 新增 `getTodoProgress(todoId)`：计算子任务完成进度
- `pages/index/index.wxss`：子任务缩进样式、删除按钮样式

验收标准：

- [ ] 详情面板中能添加子任务
- [ ] 子任务可切换完成/未完成状态
- [ ] 左滑子任务可删除
- [ ] 首页有子任务的事项显示进度（如 "2/5"）
- [ ] 删除父事项时子任务一并删除
- [ ] 保存后子任务不丢失

### 阶段 17：实现任务优先级

目标：

- 数据库 `todos` 表新增 `priority` 字段（TINYINT，默认 2）
- 详情面板增加优先级选择（高 P1 / 中 P2 / 低 P3）
- 首页事项列表按优先级排序（P1 → P2 → P3），事项左侧显示优先级色条
- 编辑面板可修改优先级

修改文件：

- `todo-api/add.php`：INSERT 语句增加 `priority` 字段，接收 `'P1'/'P2'/'P3'`
- `todo-api/update.php`（新增）：POST 接口，接收 `{id, title, description, date, category, difficulty, priority}`，UPDATE 对应记录
- `todo-api/list.php`：查询时增加 `priority` 字段，按 priority 排序
- `pages/index/index.wxml`：详情面板增加优先级选择行（三选项水平排列）；事项列表每项左侧增加色条
- `pages/index/index.js`：
  - 修改 `saveDetailTodo()`：写入 `priority` 字段
  - 修改 `editTodo()`：回填 `priority`
  - 修改 `filterTodos()`：按优先级排序
- `pages/index/index.wxss`：优先级色条样式（4rpx 宽竖条）、优先级选项按钮选中态

验收标准：

- [ ] 详情面板中可设置优先级
- [ ] 事项列表按 P1 → P2 → P3 排序
- [ ] P1 事项左侧红色色条
- [ ] P2 事项左侧橙色色条
- [ ] P3 事项左侧灰色色条
- [ ] 编辑面板可修改优先级
- [ ] 新增事项默认 P2
- [ ] 切换优先级后列表排序即时更新

## 9. UI 风格要求

整体风格参照 [todo.evestudio.cn](https://todo.evestudio.cn/)，采用简洁、清爽、轻量的设计。

### 9.1 配色方案

| 用途 | 色值 | 说明 |
|------|------|------|
| 页面背景 | `#f0f2f5` | 浅灰背景，与白色卡片形成层次 |
| 卡片/列表背景 | `#ffffff` | 纯白卡片，圆角 16rpx |
| 主色调 | `#4a90e2` | 蓝色，用于按钮、高亮、图表 |
| 主色深色 | `#3a7ac7` | 按钮/图表渐变深色端 |
| 标题文字 | `#333333` | 深灰，标题和重要文字 |
| 正文文字 | `#666666` | 中灰，次要说明文字 |
| 浅色文字 | `#999999` | 浅灰，提示和辅助信息 |
| 完成状态 | `#999999` + 删除线 | 已完成事项弱化 |
| 对勾绿色 | `#4caf50` | 完成事项前面的对勾 |

### 9.2 首页布局

```
┌──────────────────────────────┐
│  我的事项清单    趋势 总览 个人说明 │  ← 标题栏，顶头排列
├──────────────────────────────┤
│     ‹  2026-05-16  ›         │  ← 日期切换，居中
│        点击返回今天            │  ← 灰色小字提示
├──────────────────────────────┤
│  [请输入事项内容.......] [添加] │  ← 输入框 + 蓝色按钮
├──────────────────────────────┤
│  未完成          2            │  ← 分区标题 + 数量
│  ○ 学习小程序                 │  ← 未完成事项：空心圆 + 深色文字
│  ○ 整理笔记                   │
├──────────────────────────────┤
│  已完成          1            │
│  ✓ 购买日用品                 │  ← 已完成事项：绿底对勾 + 灰色删除线
└──────────────────────────────┘
```

### 9.3 组件样式规范

**页面标题栏：**
- 标题居左："我的事项清单"，字号 40rpx，加粗，深色
- 右侧文字链接："趋势" "总览" "个人说明"，字号 28rpx，蓝色

**日期切换区：**
- 箭头 `‹` `›` 字号 48rpx，深灰色
- 日期文字居中，字号 36rpx
- 下方 "点击返回今天" 字号 22rpx，浅灰色

**输入区：**
- 输入框圆角 12rpx，背景 `#f5f5f5`，内边距 20rpx
- "添加"按钮蓝色背景（`#4a90e2`），白色文字，圆角 12rpx
- 输入框和按钮水平排列，按钮固定宽度 120rpx

**事项列表：**
- 每条事项为一行，无卡片包裹，通过视觉间距区分
- 未完成事项：左侧空心圆圈（`border: 2px solid #4a90e2`），24rpx 直径
- 已完成事项：左侧绿色实心圆（`background: #4caf50`），内含白色对勾 `✓`
- 事项文字字号 30rpx，未完成用 `#333`，已完成用 `#999` + 删除线
- 事项之间通过底部细线分隔（`border-bottom: 1px solid #f0f0f0`）

**统计卡片（趋势页/总览页）：**
- 蓝色渐变背景 `linear-gradient(135deg, #4a90e2, #3a7ac7)`
- 白色文字，大数字 56rpx 加粗
- 圆角 20rpx，阴影 `0 8rpx 24rpx rgba(74,144,226,0.3)`
- 内边距 40rpx

**图表区域（趋势页）：**
- 白色卡片容器，圆角 16rpx，阴影 `0 4rpx 16rpx rgba(0,0,0,0.06)`
- 柱状图蓝色渐变，圆角顶部 8rpx
- 折线图叠加在柱状图上方，蓝色 `#4a90e2`
- Y 轴标签在左侧，浅灰色 22rpx
- X 轴日期标签在底部，浅灰色 22rpx

**月历网格（总览页）：**
- 白色卡片容器，圆角 16rpx
- 7 列网格，周日/周一为每周起始
- 日期数字居中显示
- 有事项的日期显示蓝色圆点或数量标记
- 当前月份日期为深色，非当前月份日期为浅灰色

### 9.4 字体与间距

- 字体：使用系统默认中文字体（苹方 / 微软雅黑）
- 页面左右内边距：30rpx
- 卡片间距：30rpx
- 分区之间使用细线分隔，分隔线颜色 `#eee`

### 9.5 交互反馈

- 按钮点击：轻微缩放（`scale(0.95)`）或背景色加深
- 事项点击切换：状态图标平滑过渡（圆圈 → 对勾），文字颜色渐变变灰
- 页面切换：使用小程序默认的 `wx.navigateTo` 滑动动画
- 加载状态：数据加载中显示简单 loading 或骨架，不白屏

## 10. Trae 开发要求

请 Trae 每完成一个阶段后，都输出以下内容：

```text
本阶段完成内容：

修改文件：

核心逻辑：

如何测试：

下一步建议：
```

如果出现报错，请优先修复当前问题，不要继续开发新功能。

报错处理要求：

- 先定位问题原因
- 再说明要修改哪里
- 修复后说明如何验证
- 不要在修复 bug 的同时新增其他功能

## 11. 当前优先级

优先完成：

1. 基础页面
2. 添加事项
3. 添加事项详情面板
4. 点击完成
5. 日期切换
6. 未完成和已完成分区
7. 个人说明页面
8. 样式优化
9. WampServer 本地数据管理
10. 事项总览
11. 完成事项曲线
12. 编辑事项
13. 按周查看任务
14. 子任务完整管理
15. 任务优先级

暂时不优先：

- 用户登录
- 云数据库
- 正式线上服务器部署
- 消息提醒
- 复杂日历组件
- 多人协作

## 12. 后续可扩展功能

当基础功能稳定后，可以考虑扩展：

- 删除事项
- 小程序本地缓存作为接口失败时的临时备份
- 事项到期提醒（微信订阅消息）
- 事项标签/分类自定义
- 数据导出（CSV/JSON）
- 事项搜索

这些功能暂时不作为第一版必须完成内容。
